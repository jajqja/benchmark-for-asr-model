# g-group-ai-lab/gipformer-65M-rnnt — RNNT transducer qua sherpa-onnx (OFFLINE).
# encoder/decoder/joiner/tokens để null -> adapter tự dò trong repo theo tiền tố.
# Nếu dò nhầm (repo có bản int8 + fp32), khai báo tên file rõ ràng.
name: gipformer_65m
adapter: benchmark.adapters.sherpa_onnx_transducer.SherpaOnnxTransducerAdapter
sample_rate: 16000
params:
  mode: offline
  repo_id: g-group-ai-lab/gipformer-65M-rnnt
  encoder: null
  decoder: null
  joiner: null
  tokens: null
  provider: cuda
  num_threads: 4
  decoding_method: greedy_search
